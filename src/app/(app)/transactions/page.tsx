import { TransactionsWorkspace } from "@/components/transactions/TransactionsWorkspace";

export const metadata = { title: "Transactions — PFM" };

export default function TransactionsPage() {
  return <TransactionsWorkspace />;
}
